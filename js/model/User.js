export class User {
    constructor(idUser, firstName, lastName, email, dni, password, isAdmin) {
        this.idUser = idUser
        this.firstName = firstName
        this.lastName = lastName
        this.email = email
        this.dni = dni
        this.password = password
        this.isAdmin = isAdmin
    }
}
